@model IEnumerable<DienMayQuyetTien.Areas.Admin.Models.ProductType>

@{
    Layout = null;
}
<ul class="nav navbar-nav">
    @foreach (var item in Model)
    {
        <li class="dropdown">
            <a href="" title="@item.ProductTypeName" class="dropdown-toggle" data-toggle="dropdown">@item.ProductTypeName</a>
        </li>
    }
</ul>