@model IEnumerable<DienMayQuyetTien.Areas.Admin.Models.News>
@{
    ViewBag.Title = "TinTuc";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<link href="~/Assets/Client/news/css/style.css" rel="stylesheet" />
   /* <div class="news">
    <h2>TIN TỨC & SỰ KIỆN</h2>

    <div class="myCourse_content">
        @foreach (var item in Model)
        {
            if (item.Status == true)
            {
                <div class="myCourse_item ">
                <a href="#"><img src="@item.Image" alt=""></a>
                <a href="#"><h2>@item.Title</h2></a>
                <a href="#"><p>@item.Description</p></a>
            </div>
            }  
        }
        <div class="clear"></div>
      
    </div>
</div>
</section>
<div class="line"><img src="~/Assets/Client/news/img/line.png" alt="Alternate Text" /></div>
<section class="myGallery">
    <h1>OUR GALLERY</h1>
    <div class="gallery_content">
        @foreach (var item in Model)
        {
            if (item.Status == false)
            {
                <div class="gallery_item ">
                    <a href="#"><img src="@item.Image" alt=""></a>
                    <div class="item_content">
                        <h2>@item.Title</h2>
                        <div class="underline"></div>
                        <p>@item.Description</p>
                    </div>
                </div>
            }

        }
</section>